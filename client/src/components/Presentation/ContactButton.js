import './ContactButton.scss';

const ContactButton = () => {
    return (
        <div className="ContactButton">
            <a href="#" className="ContactButton_link">Contact me</a>
        </div>
    );
};

export default ContactButton;
